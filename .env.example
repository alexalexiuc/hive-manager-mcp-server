# Google Service Account JSON (full stringified JSON)
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# Optional: pre-known spreadsheet ID to skip setup lookup on every request
SPREADSHEET_ID=

# Server
PORT=3000
